# 雪人大客户服务部客户管理系统 — 局域网共享部署教程

多台电脑共用同一份数据（报价单、物料库），只需把数据库放在一台常开电脑上共享出来，其他电脑连接即可。

---

## 一、主机设置（存放数据库的电脑，只需做一次）

> 主机要求：Windows 10，平时保持开机不关机。

### 1. 新建共享文件夹
1. 在 D 盘新建文件夹，例如：`D:\woge_share`
2. 把程序目录里的 **`woge.db`** 复制到 `D:\woge_share` 里

### 2. 开启共享
1. 右键 `D:\woge_share` 文件夹 → **属性** → **共享** 选项卡
2. 点击 **共享(S)…**
3. 下拉选择 **Everyone**（或指定用户名）→ 点 **添加**
4. 权限级别改为 **读取/写入** → 点 **共享** → 完成

### 3. 关闭密码保护（推荐，免去每台电脑输密码）
1. 打开 **控制面板** → **网络和 Internet** → **网络和共享中心**
2. 左侧 **更改高级共享设置**
3. 展开 **所有网络**：
   - 打开 **启用网络发现**
   - 打开 **启用文件和打印机共享**
   - 最下面选择 **关闭密码保护共享**
4. 保存修改

### 4. 查看并固定主机 IP
1. 按 `Win + R`，输入 `cmd` 回车
2. 输入 `ipconfig` 回车，记下 **IPv4 地址**，例如 `192.168.1.100`
3. 建议把主机 IP 在路由器里绑定固定（或电脑设置固定 IP），防止重启后 IP 变化导致其他电脑连不上

---

## 二、客户端设置（每台使用电脑的电脑都要做）

### 1. 测试能否访问主机
1. 按 `Win + R`，输入：`\\192.168.1.100`（换成你的主机 IP）→ 回车
2. 能看到 `woge_share` 文件夹即成功；如果提示输入账号密码，回到主机检查"关闭密码保护共享"是否设置

### 2. 映射网络驱动器（建议，路径固定好用）
1. 打开 **此电脑** → 上方菜单 **计算机** → **映射网络驱动器**
2. 驱动器号选 **Z:**
3. 文件夹填：`\\192.168.1.100\woge_share`
4. 勾选 **登录时重新连接** → 完成

### 3. 在系统里指定数据库
1. 打开报价系统，登录
2. 点右上角 **数据库设置** 按钮
3. 点 **选择数据库文件…**，选中 `Z:\woge.db`（即 `\\192.168.1.100\woge_share\woge.db`）
4. 提示保存成功 → **关闭程序重新打开**
5. 之后所有电脑看到的就是同一份数据了

> 以后想改回本机数据：数据库设置 → 恢复本地默认 → 重启程序。

---

## 三、自动备份说明

- 系统每天会自动备份数据库到主机 `D:\woge_share\backups\` 文件夹
- 文件名格式：`woge_20260725_090000.db`，最多保留最近 **30 份**
- 万一数据异常，把对应日期的备份文件改名为 `woge.db` 替换回去即可（先关闭所有电脑上的程序）

---

## 四、常见问题

**1. 客户端打不开 `\\192.168.1.100`**
- 检查两台电脑是否同一局域网（能互相 ping 通）
- 主机关闭防火墙试试：控制面板 → Windows Defender 防火墙 → 启用或关闭 → 专用网络选"关闭"
- 检查主机"关闭密码保护共享"是否已设置

**2. 能打开共享文件夹，但系统里读写报错**
- 共享权限要设为 **读取/写入**（主机 → 文件夹属性 → 共享 → Everyone 权限级别）

**3. 提示 database is locked（数据库被锁定）**
- 多人同一瞬间保存导致，稍等几秒重试即可
- 请避免多台电脑同时点"保存"同一张报价单

**4. 重启电脑后 Z 盘不见了 / 显示红叉**
- 确认主机已开机
- 双击此电脑里的 Z 盘重新连接；映射时务必勾选"登录时重新连接"

**5. 主机重装系统/换电脑**
- 把 `D:\woge_share` 整个文件夹（含 woge.db 和 backups）拷到新电脑，按"一、主机设置"重新共享即可
